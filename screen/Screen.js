import React from 'react';
import { ScreenContainer } from 'react-native-screens';

export const ({ Thailand }) => {
    return (
        <ScreenContainer>
            <Bu
        </ScreenContainer>
    );
}